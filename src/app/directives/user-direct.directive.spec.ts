import { UserDirectDirective } from './user-direct.directive';

describe('UserDirectDirective', () => {
  it('should create an instance', () => {
    const directive = new UserDirectDirective();
    expect(directive).toBeTruthy();
  });
});
